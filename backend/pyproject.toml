[tool.ruff]

line-length = 100

target-version = "py312"

exclude = ["**/migrations/"]

[tool.ruff.lint]

select = ["E", "F", "W", "I"]

[tool.pytest.ini_options]

DJANGO_SETTINGS_MODULE = "planty.settings"

python_files = "tests.py test_*.py"